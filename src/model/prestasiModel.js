export default class Prestasi {
  constructor(id, juara, nama, kelas, judul, deskripsi, foto_url, author_id) {
    this.id = id;
    this.juara = juara;
    this.nama = nama;
    this.kelas = kelas;
    this.judul = judul;
    this.deskripsi = deskripsi;
    this.foto_url = foto_url;
    this.author_id = author_id;
  }
}